# Views

Page-level UI (screens) live here. `app/` route files re-export these as thin entrypoints.

This folder is named `views` instead of `pages` because Next.js treats `src/pages/` as the optional Pages Router and would register every file here as a route.

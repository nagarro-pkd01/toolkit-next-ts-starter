# Layout Components

This folder contains reusable application-shell components such as navigation and footers.

Layout components may be used by templates, but they should not own page data or feature-specific
business behavior. Import the exact component file instead of using a barrel export.

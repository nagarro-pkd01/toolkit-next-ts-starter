# Performance Components

This folder contains the client performance monitor and its optional improvement panel.

The monitor integrates performance utilities with React state. The panel is dynamically loaded only
after an alert exists so its JavaScript and CSS stay outside the initial route payload.

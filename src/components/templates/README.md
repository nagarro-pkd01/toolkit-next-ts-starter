# Templates

Templates define reusable page structure, such as authentication, dashboard, shell, or product
layouts.

## How to use

Templates arrange headers, footers, navigation, and content slots. They should accept content and
configuration through props rather than fetching route data directly.

Use a template from a view when multiple screens share the same structural layout. Keep Next.js
route layouts and metadata in `src/app/`.

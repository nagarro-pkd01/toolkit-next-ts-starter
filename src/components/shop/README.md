# Shop Components

This folder contains reusable commerce UI such as product cards and cart summaries.

Keep display and interaction behavior here. Fetching, caching, and commerce integration logic
belongs in `src/services/`, while complete shop screens belong in `src/views/shop/`.

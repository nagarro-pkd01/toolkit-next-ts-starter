pre-commit:
  commands:
    lint-staged:
      run: npx lint-staged

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}

post-commit:
  commands:
    bundle-size:
      run: npm run analyze:bundle:build

pre-push:
  commands:
    validate-branch:
      run: node ./scripts/validateBranchName.mjs
    protect-main:
      run: node ./scripts/blockProtectedBranchPush.mjs
    test:
      run: npm run test
    typecheck:
      run: npm run typecheck
